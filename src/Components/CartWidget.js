const CartWidget= ({titulo, subtitulo }) => {

    return (
        <div className="CartWidget" id={"id"} >
            <h1>{titulo}</h1>
            <p>{subtitulo}</p>
        </div>
    )
}


export default CartWidget